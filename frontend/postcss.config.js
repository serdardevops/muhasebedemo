module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    // autoprefixer is typically included by react-scripts or tailwind's postcss plugin itself
    // so explicit inclusion might not be necessary unless specific features are needed.
  },
};
